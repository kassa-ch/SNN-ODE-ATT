# Models

Research-facing model organization layer.

The active package-safe implementation remains under `src/snnodeatt/`. Files under `Models/` are lightweight wrappers or mapping anchors so experiments can be discussed using the paper-facing taxonomy without breaking the active pipeline.

Server backfill status: SERVER_SOURCE_NOT_FOUND in the current local environment.
