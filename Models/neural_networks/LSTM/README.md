# LSTM

STATUS: NOT_IMPLEMENTED_OR_NOT_FOUND

No standalone LSTM/BiLSTM implementation was found during Phase R0. The DF2M baseline internally uses an LSTM predictor, but that is not a standalone LSTM baseline wrapper.
