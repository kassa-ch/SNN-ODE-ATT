# Neural Network Models

Wrappers for neural network baselines and the active SNN-ODE-ATT model.

Active implementation remains in `src/snnodeatt/models/`. This directory keeps the paper-facing method taxonomy.
