# Statistical Models

Wrappers for statistical baselines recovered in `src/snnodeatt/models/baselines/`.

Expected families: LinearAR, LassoAR, KRR, FPCA, CumInt.

`baseline.py` from the requested server source path was not accessible during Phase R0, so full source-level splitting is pending.
