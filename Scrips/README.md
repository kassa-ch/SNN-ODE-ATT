# Scrips

User-requested training-mainline organization directory.

Note: the spelling `Scrips/` is preserved to match the research plan. Package-safe active code remains in `src/snnodeatt/` and `experiments/multidata_snnodeatt_distance_benchmark/`.

`data_loader/` is the standard spelling for code. `data_lader/` is kept only as a compatibility note and should not duplicate implementation files.

STATUS: STRUCTURE_READY_SERVER_BACKFILL_PENDING
