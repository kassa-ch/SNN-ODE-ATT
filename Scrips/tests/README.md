# Scrips Tests

This directory contains the lightweight smoke tests migrated from the former top-level `tests/` path during Phase R0S pruning.

The active package implementation still lives under `src/snnodeatt/`, so the tests add the repository `src/` path explicitly until a future packaging pass removes that compatibility layer.
